{
  "name": "aws-karuta",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run -w aws-karuta dev",
    "build": "npm run -w aws-karuta build",
    "preview": "npm run -w aws-karuta preview",
    "test": "npm run -w packages/aws-karuta test run",
    "prepare": "husky install"
  },
  "author": "",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^8.0.2",
    "prettier": "^2.8.1"
  }
}
